# v0.2.0 部署状态

> 更新时间：2026-08-18（Asia/Shanghai）

## 已部署

- Git 分支：`codex/v0.2.0-workflow-ocr-scale`
- V2 代码提交：`ddfe54adfb44dff8de2f126c1d1484a80007328f`
- CloudBase 环境：`cloudbase-d4g5p82875fe1a5ce`
- CloudRun 服务：`ai-mediator-backend`
- CloudRun 版本：`003`
- CloudRun 访问方式：仅 `MINIAPP`
- CloudRun 实例：最小 2，最大 10
- 通知云函数：`sendAnalysisNotification`，状态 `Active`
- 小程序上传版本：`0.2.0`（原 AppID 的 V2 更新包；不是新小程序）
- V2 客户端策略：证据、分析、OCR 与追问强制使用新 CloudRun 链路，不自动降级到旧实现

所有服务端凭据只配置在运行环境中，不记录在本文件、Git 或小程序包内。

## 数据与验证

- 已创建 v0.2.0 所需集合和索引。
- 已迁移 51 条旧证据批次；重复 dry-run 的新增数量为 0，迁移具备幂等性。
- 腾讯高精度 OCR 对 11 张样本测试：11/11 成功，识别出 1 张完全重复图片，平均置信度 98.58%。
- 自动化测试：React 67、Mini Program 19、云函数 5、CloudRun 21，合计 112 项通过。
- `npm run build` 通过。

## 尚未执行的生产切换

- 微信公众平台仍需将已上传的 `0.2.0` 提交审核并发布；开发者工具 CLI 只能上传代码，不能代替管理员审核发布。
- `database-rules/server-only.json` 应在 `0.2.0` 正式发布后应用到业务集合。提前收紧会使仍在使用旧版客户端直写链路的用户失败。
- 正式发布并完成双账号回归后，才创建并推送 Git 标签 `v0.2.0`。

详细切换与回滚步骤见 `docs/v0.2.0-deployment-runbook.md`。
