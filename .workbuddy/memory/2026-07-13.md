# 2026-07-13 工作日志

## -604100 错误彻底修复 + 云函数全面整理

### 根因发现
- 项目有**两个 project.config.json**：根目录的和 miniprogram/ 下的
- DevTools 实际使用**根目录**的，但之前一直改的是 miniprogram/ 下的
- 根目录的 libVersion 还是 3.16.2，导致 cloud sdk injection skipped

### 修复内容
1. **根目录 project.config.json**: libVersion 3.16.2→2.32.3，新增 useApiHook/useApiHostProcess/urlCheck
2. **cloudfunctions/common/llm.js**: 完整重写，fetch→https.request，默认 deepseek，内置 fallback API key
3. **cloudfunctions/login/index.js**: v1(code2Session)→v2(getWXContext)，不再需要 wx.login code
4. **ocrImage 云函数**: 从 miniprogram/cloudfunctions/ocrImage/ 复制到 cloudfunctions/ocrImage/
5. **common/ 同步**: 已同步到所有 11 个云函数，验证 0 个 fetch() 残留
6. **app.js**: 已有 wx.cloud 防御性检查（之前已修改）
7. 写了完整的优化修复清单文档: `优化修复清单.md`

### 待用户操作
- 重新打开 DevTools 项目
- 确认基础库版本 2.32.3
- 重新部署所有云函数（尤其是 login、ocrImage、analyzeCase、chatWithAnalysis、uploadEvidence）
- 清缓存后编译测试

---

## AI 分析失败根因修复 + CloudBase CLI 部署

### llm.js 旧副本问题 (a532302)
- **根因**: `analyzeCase/common/llm.js` 是旧版本，默认 provider=anthropic + 无 FALLBACK_API_KEY + 用 fetch
- **修复**: 将 `cloudfunctions/common/llm.js` 同步到所有云函数，删除嵌套 `common/common/` 垃圾目录
- 集成测试 19/19 通过

### OCR 识别率验证
- 全量测试 11 张聊天截图：5515 字，21.1s，100% 成功率
- OCR.space API 正常，代码正常，问题在云函数部署层面

### CloudBase CLI 部署
- 安装 `@cloudbase/cli` 3.6.1，用户浏览器授权
- 创建 `cloudbaserc.json` 配置超时: ocrImage/analyzeCase=60s, uploadEvidence=15s, chatWithAnalysis=30s
- **ocrImage ✅ analyzeCase ✅ uploadEvidence ✅ chatWithAnalysis ✅** 全部部署成功

### Git 版本线
- `cc8a7da` → `a532302` (llm.js修复) → 下一步提交 cloudbaserc.json
