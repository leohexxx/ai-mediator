# 2026-07-13 工作日志

## 快速上线与用户增长架构设计

- 全面审查了 AI 调解员项目的三端代码（Web/Server/MiniProgram），确认项目当前已有完整的双人协作架构
- 识别核心矛盾：当前"需要两人才能用"的双人协作模型是最大获客障碍
- 创建了 `docs/architecture-launch-growth.md` 架构方案文档，核心策略：
  1. **Phase 1 (2周)**: 单人模式 MVP —— 上传聊天 → AI分析 → 分享卡片裂变
  2. **Phase 2 (3-4周)**: 社交裂变增强 —— 多种分享模板、话题广场、朋友圈海报
  3. **Phase 3 (5-8周)**: 双人协作模式 —— 复用现有代码

- 关键决策：
  - 单人模式：扩展 analyzeCase 云函数，confidence 自动降 15%
  - 新增 shareCard 云函数 + share_cards 集合
  - 代码复用率 ~70%，2 周可上线
  - 目标裂变系数 K > 1.2

- 暂缓上线：joinCase、generateQRCode、invite-panel、party-status、chatWithAnalysis

## MVP 单人模式代码改造（已完成）

### Git
- 改造前快照: `533656d baseline: 双人协作架构完整版本（改造前快照）`
- 改造后提交: `1956cc1 v2: MVP单人模式改造完成`

### 云函数改动（6个）
- `analyzeCase/index.js` — v2 支持单人模式：party_b 为空时仅用甲方证据，confidence 自动降 15%，新增 single_completed 状态
- `createCase/index.js` — v2 新增 mode 字段(single/dual)，单人模式不生成邀请码
- `uploadEvidence/index.js` — v2 单人模式提交后 autoAnalyze=true，支持 single_submitted 状态
- `getCaseDetail/index.js` — v2 兼容单人模式(party_b null)，新增 isSingleMode 返回字段
- `shareCard/index.js` — 🆕 新增：获取分析数据 + 生成带参小程序码 + 返回卡片数据
- `cloudfunctions/common/package.json` — 🆕 解决 root package.json type:module 导致的 CJS 加载问题

### 小程序前端改动（7个）
- `app.js` — v2 简化：移除邀请码解析逻辑，改为 pendingCaseId
- `app.json` — 调整页面顺序(index→upload→report 优先)
- `pages/index/*` — v2 重写：Hero CTA 一键创建案例→跳转上传，案例列表增加状态标签
- `pages/upload/upload.js` — v2 单人模式提交后自动触发分析并跳转报告页
- `pages/report/*` — v2 增加分享按钮+分享面板(4种模板)+分享配置
- `services/case.js` — v2 新增 getShareCard()，createCase 增加 mode 参数
- `utils/format.js` — v2 新增 single_submitted/single_completed 状态 label

### 新组件
- `components/share-card/*` — 🆕 Canvas 2D 绘制分享卡片(verdict/fun/suspense三种模板)，支持保存相册

### 测试
- `miniprogram/__tests__/mvp-integration.test.cjs` — 🆕 19 项测试全部通过
- `miniprogram/package.json` — 🆕 解决 ESM/CJS 模块解析
