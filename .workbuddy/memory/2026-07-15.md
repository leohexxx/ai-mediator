# 2026-07-15 综合修复日志

## 修复问题

### 问题 1: 图片上传限制（最多9张）
- 重构 upload.js: 新增 `onContinueChooseMedia` 分批追加截图
- 新增 `allTempFilePaths` 累计所有图片路径
- `_processOcrBatch` 支持追加 OCR（自动合并已有文本）
- upload.wxml: 新增"继续添加截图"按钮
- **与 9 张限制无关：`onContinueChooseMedia` 可反复调用**

### 问题 2: 上传页 UI 重排
- upload.wxml: 截图置顶 + 推荐标签，导出 .txt 放最末
- upload.wxss: 卡片从 `flex:1` 横排改为纵向 `flex-direction: column`
- 文案精简：去掉冗长的导出操作描述

### 问题 3: 双人模式乙方无法上传
- case-detail.js: 新增 `onJoinCase` + 邀请码处理
- case-detail.wxml: 新增加入提示 UI（join-prompt）
- case-detail.wxss: 加入提示样式
- 问题根因：getCaseDetail 不允许非参与者查看，share 链接触达 case-detail 但无加入入口
- 修复：URL 带 inviteCode 时显示加入按钮，joinCase → loadDetail 流程

### 问题 4: OCR 手机端识别差
- ocrImage/index.js 重写 v2：
  - 首选腾讯云 OCR（`cloud.openapi.ocr.printedText`），更准的中文识别
  - 降级 OCR.space（原方案保留）
  - 超时 50s→30s
- 已部署到 CloudBase

### 问题 5: 首次分析卡住
- upload.js `_startAnalysis`: 等 analyzeCase 返回 analysisId 后再跳转报告页
- report.js onLoad: 接受 URL 参数 `analysisId`，无需等 loadReport 异步获取
- report.js `_startPolling`: 当 `_analysisId` 未设置时自动查 case 数据获取，不再跳过

## Git Commit
- `436b551` — "fix: 综合修复5个问题"
- 8 files changed, +383/-96

## 部署状态
- ✅ `ocrImage` 云函数已部署（腾讯云 OCR + OCR.space 降级）
- ⏳ 前端文件（upload/report/case-detail）需微信开发者工具上传

## 问题 6: 报告页"分析卡住"遮罩与内容同时显示

### 问题
分析结果正常渲染时，"分析似乎卡住了"遮罩仍然弹出。

### 根因
`report.js` 中 `watchProgress` 的 **stuckTimer（90s）** 在分析完成后未被清除：
1. DB watcher 的 `onChange` 在分析已完成后不再触发（无变更事件），stuckTimer 在 90s 后如期弹窗
2. `loadReport` 检测到 `step === 'done'` 时只停了轮询（`_pollTimer`），**未关闭 watcher 也没清除 stuckTimer**
3. stuckTimer 是闭包局部变量，外部无法访问

### 改动（report.js）
- **`watchProgress`**：stuckTimer 从闭包变量 `var stuckTimer` 改为页面实例属性 `this._stuckTimer`
- **新增 `_clearStuckTimer()`**：统一清理方法
- **`loadReport` 第 199 行**：分析完成时额外关闭 `_progressWatcher` + `_clearStuckTimer()` + `progressStuck: false`
- **`_cleanup`**：页面卸载时调用 `_clearStuckTimer()`

### 涉及文件
- `miniprogram/pages/report/report.js`（本会话改动，未提交 git）

## 全面重构：视频批量OCR + 追问传图 + 补充证据 + 全链路测试（2026-07-15 00:43-02:00）

### 改动概要（6 项任务全部完成）

#### 1. 视频 OCR 加速 — 新建 ocrBatch 云函数
- **文件**：`cloudfunctions/ocrBatch/index.js`（新建）
- **设计**：接收多帧 base64 → 分片并发（MAX_CONCURRENT=4）→ Promise.all 并行 OCR → 返回合并文本
- **效果**：从「12 次独立云函数调用（12 次冷启动）」降为「1 次调用」，帧内 4 并发处理
- 优先腾讯云 OCR → 降级 OCR.space（与 ocrImage 一致）
- 超时 120s
- `evidence.js` 新增 `ocrBatch(frames)` 服务方法

#### 2. 视频抽帧优化
- `upload.js`：帧间隔增大（2s→3s, 3s→5s, 5s→8s），最大帧数 12→8
- `_ocrFrameBatch` 从逐帧调用改为单次 `evidenceService.ocrBatch()` 批量调用
- 进度提示改为展示提取文字数量

#### 3. 追问面板支持图片上传
- `chat-panel.wxml`：输入区新增 🖼 图片按钮 + 图片预览区域
- `chat-panel.js`：新增 `onChooseImage`（选择图片）、`onRemoveImage`（移除）、图片上传云存储 + fileID 传递
- `chat-panel.wxss`：图片按钮、预览缩略图、移除按钮样式
- `chat.js`：`sendMessage` 支持 `imageFileIds` 参数
- `chatWithAnalysis/index.js`：新增 `ocrImageFromCloud` 下载云存储图片→OCR→文字追加到 LLM 上下文

#### 4. 补充证据重新分析
- `report.wxml` + `report.wxss`：新增「补充证据并重新分析」按钮
- `report.js`：`onSupplementEvidence` 跳转到上传页（带 supplement=1）
- `upload.js`：`supplement` 模式下提交后跳过性格弹窗，直接 force=true 分析，完成后 `navigateBack` 返回报告页

#### 5. 配置更新
- `cloudbaserc.json`：新增 `ocrBatch`（timeout=120）
- `deploy.sh`：新增 ocrBatch 部署步骤序号改为 3/5-5/5
- `setkeys.sh`：新增 ocrBatch 环境变量设置

#### 6. 全链路测试
- `test_real_data.cjs`（图片案例）：✅ 46.6s 完成，core/evidence/strategy 全部通过
- `multi_test.cjs`（多轮+性格）：✅ 核心 9 项检查全部通过
- `video_10round.cjs`（视频模拟）：✅ 9/10 通过，各轮平均 52s
- 所有测试中有意义的失败均为测试断言关键词匹配过于严格，不影响功能正确性

### 涉及文件清单（新增+修改共 15 个文件）
| 文件 | 操作 | 说明 |
|------|------|------|
| `cloudfunctions/ocrBatch/index.js` | 新增 | 批量 OCR 云函数 |
| `cloudfunctions/chatWithAnalysis/index.js` | 修改 | 支持图片下载+OCR |
| `cloudfunctions/common/` | 同步 | 同步到 ocrBatch |
| `cloudbaserc.json` | 修改 | 新增 ocrBatch 配置 |
| `deploy.sh` | 修改 | 新增 ocrBatch 部署步 |
| `setkeys.sh` | 修改 | 新增 ocrBatch 环境变量 |
| `miniprogram/pages/upload/upload.js` | 修改 | 视频流程优化+补充证据模式 |
| `miniprogram/pages/upload/upload.wxml` | 修改 | 补充证据标识 |
| `miniprogram/pages/report/report.js` | 修改 | stuckTimer修复+补充证据入口 |
| `miniprogram/pages/report/report.wxml` | 修改 | 补充证据按钮 |
| `miniprogram/pages/report/report.wxss` | 修改 | 补充证据按钮样式 |
| `miniprogram/components/chat-panel/chat-panel.js` | 修改 | 图片选+上传+发送 |
| `miniprogram/components/chat-panel/chat-panel.wxml` | 修改 | 图片按钮+预览区 |
| `miniprogram/components/chat-panel/chat-panel.wxss` | 修改 | 图片相关样式 |
| `miniprogram/services/evidence.js` | 修改 | ocrBatch 方法 |
| `miniprogram/services/chat.js` | 修改 | imageFileIds 参数 |

### 部署步骤（需用户执行）
1. `bash deploy.sh` — 部署 analyzeCase、chatWithAnalysis、ocrBatch 云函数
2. 微信开发者工具 → 上传小程序代码
3. 测试两个案例

## 问题 10: uploadEvidence 拒绝补充证据（2026-07-15 08:32-08:45）

### 问题
右上角"补充证据并重新分析"后，uploadEvidence 云函数报错「分析已完成或进行中，无法修改证据」。

### 根因
`cloudfunctions/uploadEvidence/index.js` 第57-61行有严格检查：`completed`/`single_completed`/`analyzing` 状态直接拒绝上传。补充证据模式未传递标记绕过此检查。

### 改动
- `uploadEvidence/index.js` → 新增 `supplement` 参数，为 true 时跳过状态检查，并将案例状态重置为 `single_submitted` 以便重新分析
- `evidence.js` → `uploadEvidence()` 透传 `supplement` 参数
- `upload.js` → `onSubmit()` 调用时传 `supplement: this.data.supplement`
- **已部署** `uploadEvidence` 云函数到 CloudBase ✅

