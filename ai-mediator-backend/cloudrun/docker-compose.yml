services:
  backend:
    build: .
    ports:
      - "9000:9000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - LOCAL_MODE=true
