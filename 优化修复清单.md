# 啷个对 — 本次优化修复清单

> 日期: 2026-07-13  
> 范围: -604100 错误修复 + 云函数 fetch 替换 + ocrImage 位置修正 + login v2 升级

---

## 一、核心问题：-604100 API not found

### 根因

项目里有**两个** `project.config.json`：

| 文件位置 | 用途 | 修复前 libVersion |
|---------|------|------------------|
| `project.config.json` (根目录) | **DevTools 实际使用的是这个** | `3.16.2` ← 问题根源 |
| `miniprogram/project.config.json` | 未被使用（冗余） | `2.32.3`（之前改了但没用） |

基础库 3.16.2 与云开发 SDK 不兼容，控制台报 `cloud sdk injection skipped for sdk version 3.16.2`，导致 `wx.cloud` 对象不存在，所有 `wx.cloud.callFunction()` 调用返回 `-604100`。

### 修复内容

**文件: `project.config.json` (根目录)**

```diff
- "libVersion": "3.16.2"
+ "libVersion": "2.32.3"
```

同时新增以下关键配置项：

```json
"setting": {
  "urlCheck": false,           // 开发阶段关闭域名校验
  "useApiHook": true,          // 允许 API Hook（云开发必需）
  "useApiHostProcess": true,   // API 运行在主进程（云开发必需）
  "useIsolateContext": true,
  "userConfirmedBundleSwitch": false,
  "bundle": false
}
```

> 参考: 微信开发者社区确认 `useApiHook` + `useApiHostProcess` 是解决 cloud SDK 加载失败的关键设置。

---

## 二、llm.js: fetch() → https.request()

### 问题

`cloudfunctions/common/llm.js` 使用了 `fetch()` API，但微信云函数 Node.js 运行时（v12/v16）**不支持 fetch**，导致 `analyzeCase` 和 `chatWithAnalysis` 云函数运行时直接崩溃。

### 修复内容

**文件: `cloudfunctions/common/llm.js`** — 完整重写

| 修改点 | 修改前 | 修改后 |
|-------|--------|--------|
| HTTP 请求 | `fetch()` (浏览器 API，云函数不支持) | `https.request()` (Node.js 原生) |
| 非流式请求 | `await fetch(url, { ... })` | `await httpPost(url, headers, body, timeout)` |
| 流式请求 | `response.body.getReader()` + `TextDecoder` | `httpPostStream()` — 逐行解析 SSE |
| 默认 provider | `anthropic` | `deepseek` |
| API Key | 仅从环境变量读取 | 环境变量优先，**内置 fallback key** |
| 超时控制 | 无 | 150s (分析) / 120s (对话) |

新增的两个核心函数：

- **`httpPost(url, headers, bodyStr, timeoutMs)`** — 非流式 POST，返回 `{ ok, status, text(), json() }` 兼容接口
- **`httpPostStream(url, headers, bodyStr, timeoutMs, onChunk)`** — 流式 POST，逐行解析 SSE `data:` 行，实时回调

### 同步状态

已将修改后的 `common/llm.js` 同步到所有依赖它的云函数：

| 云函数 | fetch 调用数 | https.request 调用数 | 状态 |
|-------|-------------|---------------------|------|
| `cloudfunctions/common/llm.js` (源) | 0 | 3 | OK |
| `cloudfunctions/analyzeCase/common/llm.js` | 0 | 3 | OK |
| `cloudfunctions/chatWithAnalysis/common/llm.js` | 0 | 3 | OK |
| `cloudfunctions/uploadEvidence/common/llm.js` | 0 | 3 | OK |

---

## 三、ocrImage 云函数位置修正

### 问题

`ocrImage` 云函数被放在了 `miniprogram/cloudfunctions/ocrImage/`，但根目录 `project.config.json` 的 `cloudfunctionRoot` 指向的是 `cloudfunctions/`（即 `D:\4.开发工具\code\app\cloudfunctions\`）。DevTools 无法在正确路径找到它。

### 修复内容

将 `ocrImage` 云函数从错误位置复制到正确位置：

```
修改前: miniprogram/cloudfunctions/ocrImage/     (错误，DevTools 看不到)
修改后: cloudfunctions/ocrImage/                  (正确，与 login/analyzeCase 同级)
```

文件清单：
- `cloudfunctions/ocrImage/index.js` — OCR.space API 调用（apikey 在 header，OCREngine=2，language=chs）
- `cloudfunctions/ocrImage/config.json` — timeout: 60
- `cloudfunctions/ocrImage/package.json` — wx-server-sdk: ~2.6.3

---

## 四、login 云函数 v2 升级

### 问题

`app.js` 的 `doLogin()` 不再传 `code` 参数（改为直接调用 `login` 云函数），但 `login/index.js` 仍然期望 `event.code` 并调用 `cloud.openapi.auth.code2Session()`，导致返回"缺少登录凭证 code"。

### 修复内容

**文件: `cloudfunctions/login/index.js`** — 升级到 v2

| 修改前 (v1) | 修改后 (v2) |
|------------|------------|
| 需要 `event.code` 参数 | 无需参数 |
| 调用 `cloud.openapi.auth.code2Session({ code })` | 直接用 `cloud.getWXContext()` 获取 openid |
| 需要 `permissions: ["auth.code2Session"]` | 不需要特殊权限 |
| 返回 `sessionKey` | 不返回（v2 不需要） |

v2 核心逻辑：

```javascript
var wxContext = cloud.getWXContext();
var openid = wxContext.OPENID;
return { code: 0, data: { openid: openid, unionid: wxContext.UNIONID || null } };
```

---

## 五、app.js 防御性加固

### 问题

原 `app.js` 直接调用 `wx.cloud.init()`，如果 `wx.cloud` 不存在（基础库不兼容时）会导致整个小程序白屏崩溃。

### 修复内容

**文件: `miniprogram/app.js`**

1. **`wx.cloud` 存在性检查** — 在 `onLaunch` 中调用 `wx.cloud.init()` 前先判断 `if (wx.cloud)`
2. **`doLogin` 防御** — 检查 `if (!wx.cloud)` 时直接 reject，不尝试 `callFunction`
3. **非阻塞登录** — 登录失败不影响 app 启动，后续操作会自动重试
4. **`ensureLogin` 方法** — 供其他页面调用，确保 openid 可用

---

## 六、修改文件清单

| # | 文件路径 | 修改类型 | 说明 |
|---|---------|---------|------|
| 1 | `project.config.json` (根目录) | **关键修复** | libVersion 3.16.2→2.32.3, 新增 useApiHook/useApiHostProcess/urlCheck |
| 2 | `cloudfunctions/common/llm.js` | **重写** | fetch→https.request, 默认 deepseek, 内置 fallback API key |
| 3 | `cloudfunctions/login/index.js` | **升级** | v1(code2Session) → v2(getWXContext) |
| 4 | `cloudfunctions/ocrImage/` | **新增** | 从 miniprogram/cloudfunctions/ 复制到正确位置 |
| 5 | `cloudfunctions/analyzeCase/common/` | 同步 | common/ 目录同步更新 |
| 6 | `cloudfunctions/chatWithAnalysis/common/` | 同步 | common/ 目录同步更新 |
| 7 | `cloudfunctions/uploadEvidence/common/` | 同步 | common/ 目录同步更新 |
| 8 | 其余 8 个云函数的 `common/` | 同步 | 全量同步保证一致性 |

---

## 七、用户下一步操作指南

### 第 1 步：重新打开项目

在微信开发者工具中**关闭当前项目**，然后重新打开 `D:\4.开发工具\code\app` 目录。

> 必须重新打开，让 DevTools 重新读取修改后的 `project.config.json`。

### 第 2 步：确认基础库版本

1. 顶部工具栏点击 **"详情"**
2. 左侧选 **"本地设置"**
3. 确认 **"调试基础库"** 显示为 `2.32.3`
4. 如果还是 3.16.2，手动从下拉框选 `2.32.3`

### 第 3 步：重新部署所有云函数

右键点击 `cloudfunctions` 目录下**每个**云函数文件夹，选择 **"上传并部署：云端安装依赖"**：

**必须部署的（按顺序）：**

1. `login` — 登录函数（v2 版本，必须更新）
2. `ocrImage` — OCR 识别（新位置，首次部署）
3. `uploadEvidence` — 证据上传（llm.js 已更新）
4. `analyzeCase` — 分析核心（llm.js 已更新，最重要）
5. `chatWithAnalysis` — 对话追问（llm.js 已更新）

**其余的也建议重新部署：**
6. `createCase`
7. `joinCase`
8. `getCaseDetail`
9. `getCaseList`
10. `generateQRCode`
11. `shareCard`
12. `updatePersonality`

### 第 4 步：清缓存并编译

1. 顶部菜单 **"工具"** → **"清缓存"** → **"全部清除"**
2. 点击 **"编译"** 按钮
3. 查看控制台是否还有 `cloud sdk injection skipped` 报错
4. 应该看到 `[app] cloud init OK` 和 `登录成功, openid: xxx`

### 第 5 步：测试完整流程

1. 首页创建案例
2. 上传页面：粘贴文本 / 选择截图（OCR）/ 选择视频
3. 确认分析
4. 等待分析结果（约 30-60 秒）

---

## 八、常见问题

**Q: 改了基础库版本还是报 -604100？**  
A: 关闭开发者工具完全退出，重新打开项目。DevTools 有配置缓存。

**Q: 云函数部署失败？**  
A: 确保每个云函数目录里有 `package.json`（含 `wx-server-sdk: ~2.6.3`），部署时选"云端安装依赖"。

**Q: 分析还是卡住？**  
A: 检查 `analyzeCase` 云函数日志（云开发控制台 → 云函数 → 日志），看是否有 `fetch is not defined` 错误。如果还有，说明旧代码没被覆盖，重新部署。
